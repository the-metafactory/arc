---
name: Devkit
description: Bundle-style skill repo shipping a CLI plus slash commands.
---

# Devkit

A passing bundle fixture: `type: bundle` with a `provides.cli` + `provides.commands`
block. Manifest name `devkit` derives from `metafactory-bundle-devkit`, and this
frontmatter name is its PascalCase.
